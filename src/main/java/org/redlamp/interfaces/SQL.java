package org.redlamp.interfaces;

public interface SQL {

	int MOBILE_SERVICE = 44;

	String LOAN_ACCT_LIST = "{call Nep_Loan_Acct_List ? }";

	String MOB_CALLABLE = "{? = call psp_ex_main2 (" + MOBILE_SERVICE
			+ ",?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)}";

	String VALIDATE_TXN = "{? = call psp_ex_validate_txn (" + MOBILE_SERVICE + ",?,?,?,?,?,?,?,?,?,?,?,?,?,?)}";

	String LOAN_BILL_SCHEDULE = "{call Nep_Loan_Bill_Schedule ? }";

	String DEPOSIT_ACCT_LIST = "{call Nep_Dep_Acct_List ? }";

	String INIT_SERVICE_REGISTRATION = "{call Nep_Check_Service ?}";

	String COMPLETE_SERVICE_REGISTRATION = "{ ? = call csp_add_eb_service ?,?,?,?}";

	String SEND_SMS = "{ ? = call Nep_Send_SMS ?,?,?,?,? }";

	String LOAN_DISPLAY = "{call Nep_ln_display ?, ?}";

	String MOBILE_MINI_STMT = "{ call Mob_Mini_Stmt (?,?) }";

	String DP_ACCT_CREATION = "{  call Nep_Dp_Cre_OL (?,?,?,?,?,?) }";
	String CUSTOMER_CREATION = "{ call Nep_Cust_Cre_OL (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?) }";
}
